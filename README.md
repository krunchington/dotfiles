# Dotfiles

My dotfiles setup.

## Prerequisites

1. Have homebrew (mac) or pacman (arch)
2. install `mise`

## Setting up from scratch

```
cd ~
mkdir -p dev
cd dev
git clone git@github.com:krunchington/dotfiles.git
cd dotfiles
./setup.sh
```
